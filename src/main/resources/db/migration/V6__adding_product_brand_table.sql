

CREATE TABLE product_brand (
                                 id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                 name VARCHAR(255),
                                 image LONGBLOB NOT NULL
);
